<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @recipe.name %>
</p>

<!-- This is showing the wrong ingredients (not the newly imported in the nested forms) -->
<p>
  <strong>Ingredients:</strong>
  <% @ingredient.recipe.each do |recipe| %>
  <tr>
    <td><%= ingredient.ingredient %></td>
  </tr>
  <% end %>
</p>

<p>
  <strong>Description:</strong>
  <%= @recipe.description %>
</p>

<%= link_to 'Edit', edit_recipe_path(@recipe) %> |
<%= link_to 'Back', root_path %>
